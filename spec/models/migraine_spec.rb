require 'spec_helper'

describe Migraine do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Migraine.create!(@valid_attributes)
  end
end
